import React from "react";
import { Section } from "../Section";
import { Title } from "../Title";

const Schedule = () => {
  return <Section title={<Title title={"schedule Lorem Ipsum:"} />} />;
};

export default Schedule;
